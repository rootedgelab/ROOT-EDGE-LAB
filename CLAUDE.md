# サイト健診事業 — Claude 作業ルール

このリポジトリは、しょーた（名古屋のWeb制作者）の「サイト健診事業」の業務基盤。
営業リスト生成〜健診納品までを「しょーたの承認だけで回る仕組み」として運用する。
このファイルのルールは全セッション・全工程で厳守すること。

## 大原則（ガードレール）

1. **ファイルの削除は一切しない。** 移動・上書きは `健診事業/scripts/safe_move.py` を使い、
   「元→新」の復元ログを `健診事業/logs/復元ログ.csv` に全件記録する。
2. **外部への送信（DM・メール・投稿）は絶対に自動化しない。** 文面の下書きまでで止め、
   送信は必ずしょーたが行う。
3. **画面スクレイピングは禁止。** 店舗データの取得は公式の Google Places API (New) 経由のみ。
   （リスト通過店舗のサイトのトップページ1回のみの機械チェックは可。間隔1秒以上）
4. 取得した店舗データは営業目的の社内利用に限定。第三者提供・公開はしない。
5. 生成物に効果保証・恐怖訴求の文言を入れない（「予約が増える」等の断定は禁止）。
6. 各フェーズの完了時に必ずしょーたの承認を取ってから次へ進む。
7. 不明点は勝手に仮定で埋めず、作業前にまとめて質問する。
8. APIキー等の秘密情報は `.env` のみに保存し、コードに直書きせず、コミットしない。

## 正本（すべての判断基準）

`健診事業/正本/` の3ファイル（商品設計書・監査正本プロンプト・営業文セット）が
監査・営業文・計画書すべての判断基準。**作業前に必ず全文を読むこと。**
まだ置かれていない場合は、しょーたに依頼して待つ（勝手に代用しない）。

## 定型の合図

- **「今日なにやる？」** と言われたら:
  `健診事業/リスト/` のCSVステータス・`健診事業/案件/` の進捗・納品期限を読み、
  `健診事業/司令書/{日付}.md` に司令書を1枚出力する。
  補助スクリプト: `python3 健診事業/scripts/make_briefing.py`
  （スクリプトの出力を土台に、状況を読んで加筆してよい）
- **CSV上位の店名を指定されたら**（フェーズ3・DM下書き）:
  正本/営業文セット.md の「B. 新規向けDM」の型で、そのサイトの実内容から確認できた
  具体的な指摘を2点入れた下書きを `健診事業/リスト/dm_drafts/{店名}.md` に保存し、
  CSVのステータスを「下書き済」に更新する。指摘の憶測は禁止。送信はしない。

## 詳しい使い方

`健診事業/README.md` を参照。
